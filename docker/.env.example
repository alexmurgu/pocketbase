# Values are used by docker-compose.yml. Use a URL-safe password because it is
# embedded in the PostgreSQL connection URL.
POSTGRES_USER=pocketbase
POSTGRES_PASSWORD=replace-with-a-strong-url-safe-password
POSTGRES_DB=postgres
