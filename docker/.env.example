# docker-compose.yml embeds this password in the PostgreSQL connection URL.
# Use a URL-safe value.
POSTGRES_USER=pocketbase
POSTGRES_PASSWORD=replace-with-a-strong-url-safe-password
POSTGRES_DB=postgres
